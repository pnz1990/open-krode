---
description: Build and install the open-krode plugin
---
Run the install:plugin script and report the result:
!`bun run install:plugin 2>&1`

If the build succeeded, confirm it's installed. If it failed, show the errors and fix them.
